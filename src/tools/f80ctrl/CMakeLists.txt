# PROJECT(as3dctrl)

ADD_DEFINITIONS(
)

INCLUDE_DIRECTORIES(
)

SET(HEADERS
  f80ctrl.h
)

SET(SOURCES
  f80ctrl.cpp
)

IF(UNIX)
  FIND_LIBRARY(RT rt)
  IF(RT)
    SET(EXTRA_LIBS ${RT})
  ENDIF()
ENDIF()

ADD_COVISE_EXECUTABLE(f80ctrl)

TARGET_LINK_LIBRARIES(f80ctrl coNet ${EXTRA_LIBS})

COVISE_INSTALL_TARGET(f80ctrl)
