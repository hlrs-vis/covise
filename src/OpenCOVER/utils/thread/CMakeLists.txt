file(GLOB_RECURSE SOURCES "*.cpp")
file(GLOB_RECURSE HEADERS "*.h")

ADD_COVISE_LIBRARY(coThreadUtil ${SOURCES})
COVISE_INSTALL_TARGET(coThreadUtil)
COVISE_INSTALL_HEADERS(coThreadUtil ${HEADERS})
COVISE_WNOERROR(coThreadUtil BASEARCH zebu)
set_target_properties(coThreadUtil PROPERTIES LINKER_LANGUAGE CXX POSITION_INDEPENDENT_CODE ON)