USING(OpenSceneGraph)

ADD_DEFINITIONS(
)

INCLUDE_DIRECTORIES(
  ${OPENSCENEGRAPH_INCLUDE_DIRS}
)


SET(SOURCES
  fixLOD.cpp
)


ADD_COVISE_EXECUTABLE(fixLOD)

TARGET_LINK_LIBRARIES(fixLOD coUtil ${EXTRA_LIBS} ${OPENSCENEGRAPH_LIBRARIES} )

COVISE_INSTALL_TARGET(fixLOD)
