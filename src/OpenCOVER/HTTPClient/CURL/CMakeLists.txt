USING(CURL)

SET(SOURCES
  request.cpp
)

SET(HEADERS
  export.h
  request.h
)
ADD_COVISE_LIBRARY(coCurlHTTPClient SHARED ${EXTRASOURCES} ${SOURCES} ${HEADERS})


# TARGET_INCLUDE_DIRECTORIES(coOpenOpcUaClient PRIVATE ${OPENSCENEGRAPH_INCLUDE_DIRS})

COVISE_WNOERROR(coCurlHTTPClient BASEARCH zebu)