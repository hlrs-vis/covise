using(MIDIFILE)
using(SDL2)
using(FFTW)
using(VRML)
IF(WIN32)
  message("noAlsa")
ELSE(WIN32)
using(ALSA)
ENDIF(WIN32)
#using(OPEN62541)
#USE_MIDIFILE()
#    MESSAGE(${MIDIFILE_INCLUDE_DIR})
#    INCLUDE_DIRECTORIES(${MIDIFILE_INCLUDE_DIR})
SET(HEADERS
  midiPlugin.h
)
SET(SOURCES
  midiPlugin.cpp
)
cover_add_plugin(Midi)
IF(WIN32)
 TARGET_LINK_LIBRARIES(Midi winmm 	Iphlpapi coOpenOpcUaClient ${EXTRA_LIBS})
ENDIF(WIN32)
TARGET_LINK_LIBRARIES(Midi coOpenOpcUaClient ${EXTRA_LIBS})

#COVER_INSTALL_PLUGIN(MIDI)
