USING(XENOMAI)
if(NOT XENOMAI_FOUND OR NOT COVISE_USE_XENOMAI)
  RETURN()
endif(NOT XENOMAI_FOUND OR NOT COVISE_USE_XENOMAI)

IF(NOT COVISE_BUILD_DRIVINGSIM)
  RETURN()
ENDIF()

SET(HEADERS
 HMIDevicesPlugin.h
)
SET(SOURCES
 HMIDevicesPlugin.cpp
)

COVISE_FIND_PACKAGE(V8)
INCLUDE_DIRECTORIES(
   ../../../VehicleUtil
   ${V8_INCLUDE_DIR}
)

cover_add_plugin_TARGET(HMIDevices)

TARGET_LINK_LIBRARIES(HMIDevices
   coOpenVehicleUtil
)
COVISE_WNOERROR(HMIDevices)
COVER_INSTALL_PLUGIN(HMIDevices)
