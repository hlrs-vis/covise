USING(GDAL)
USING(OSGTERRAIN)
USING(XERCESC optional)

INCLUDE_DIRECTORIES(../../include {OPENSCENEGRAPH_INCLUDE_DIRS} )

SET(SOURCES vpbcache.cpp )

ADD_COVISE_EXECUTABLE(vpbcache ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(vpbcache coOpenVehicleUtil coVPB ${OPENSCENEGRAPH_LIBRARIES})

COVISE_INSTALL_TARGET(vpbcache)

