USING(GDAL)
USING(OSGTERRAIN)
USING(XERCESC optional)
USING(V8)
USING(VehicleUtil)

INCLUDE_DIRECTORIES({OPENSCENEGRAPH_INCLUDE_DIRS} ../../include)

SET(SOURCES osgdem.cpp )

ADD_COVISE_EXECUTABLE(osgdem ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(osgdem coVPB ${OPENSCENEGRAPH_LIBRARIES})

COVISE_INSTALL_TARGET(osgdem)

