USING(GDAL)
USING(OSGTERRAIN)
USING(XERCESC optional)
COVISE_FIND_PACKAGE(V8)
if(NOT V8_FOUND)
   return()
endif()


INCLUDE_DIRECTORIES(${V8_INCLUDE_DIR} ../../include ../../../VehicleUtil  {OPENSCENEGRAPH_INCLUDE_DIRS} )

SET(SOURCES osgdem.cpp )

ADD_COVISE_EXECUTABLE(osgdem ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(osgdem coOpenVehicleUtil coVPB ${OPENSCENEGRAPH_LIBRARIES})

COVISE_INSTALL_TARGET(osgdem)

