USING(GDAL)
USING(OSGTERRAIN)
USING(XERCESC optional)

INCLUDE_DIRECTORIES(../../include {OPENSCENEGRAPH_INCLUDE_DIRS} )

SET(SOURCES vpbsizes.cpp )

ADD_COVISE_EXECUTABLE(vpbsizes ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(vpbsizes coOpenVehicleUtil coVPB ${OPENSCENEGRAPH_LIBRARIES})

COVISE_INSTALL_TARGET(vpbsizes)

