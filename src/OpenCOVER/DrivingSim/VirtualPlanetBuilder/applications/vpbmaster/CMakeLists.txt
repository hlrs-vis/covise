USING(GDAL)
USING(OSGTERRAIN)
USING(XERCESC VTK optional)

INCLUDE_DIRECTORIES(../../include {OPENSCENEGRAPH_INCLUDE_DIRS} )

SET(SOURCES vpbmaster.cpp)

ADD_COVISE_EXECUTABLE(vpbmaster ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(vpbmaster coOpenVehicleUtil coVPB ${OPENSCENEGRAPH_LIBRARIES})

COVISE_INSTALL_TARGET(vpbmaster)

