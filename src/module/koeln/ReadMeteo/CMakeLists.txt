USING(Virvo)

SET(HEADERS
)
SET(SOURCES
  ReadMeteo.cpp
)
ADD_COVISE_MODULE(IO ReadMeteo ${EXTRASOURCES})
TARGET_LINK_LIBRARIES(ReadMeteo coApi coAppl coCore ${EXTRA_LIBS})
COVISE_INSTALL_TARGET(ReadMeteo)
