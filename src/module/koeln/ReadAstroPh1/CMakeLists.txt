COVISE_FIND_PACKAGE(NetCDF)

IF (NOT NETCDF_FOUND)
  RETURN()
ENDIF (NOT NETCDF_FOUND)

INCLUDE_DIRECTORIES(
${NETCDF_INCLUDE_DIR}
)

SET(HEADERS
)
SET(SOURCES
  ReadAstro.cpp
)
ADD_COVISE_MODULE(IO ReadAstroPh1 ${EXTRASOURCES})
TARGET_LINK_LIBRARIES(ReadAstroPh1 coApi coAppl coCore ${NETCDF_LIBRARIES})
COVISE_INSTALL_TARGET(ReadAstroPh1)
