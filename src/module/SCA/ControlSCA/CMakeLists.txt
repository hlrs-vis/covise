INCLUDE_DIRECTORIES(
  ../SCA_DataBase
)
SET(HEADERS
)
SET(SOURCES
  ControlSCA.cpp
)

ADD_COVISE_MODULE(SCA ControlSCA ${EXTRASOURCES})
TARGET_LINK_LIBRARIES(ControlSCA coApi coAppl coCore coAlg SCADataBase)
COVISE_INSTALL_TARGET(ControlSCA)
