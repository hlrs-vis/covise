USE_VTK()

SET(HEADERS
)
SET(SOURCES
  PolyToTetraUSG.cpp
)

ADD_COVISE_MODULE(Tools PolyToTetraUSG ${EXTRASOURCES})
TARGET_LINK_LIBRARIES(PolyToTetraUSG coApi coAppl coCore ${EXTRA_LIBS})
COVISE_INSTALL_TARGET(PolyToTetraUSG)
