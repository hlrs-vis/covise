USING(Steereo)
USING(MPI)

SET(HEADERS
  LS1.h
)
SET(SOURCES
  LS1.cpp
)
ADD_COVISE_MODULE(HLRS MDRender ${EXTRASOURCES})
TARGET_LINK_LIBRARIES(MDRender coApi coAppl coCore ${EXTRA_LIBS}) 
COVISE_INSTALL_TARGET(MDRender)
