USING(NETCDF)

SET(HEADERS
  ReadECMWF.h
)

SET(SOURCES
  ReadECMWF.cpp
)

ADD_COVISE_MODULE(IO ReadECMWF ${EXTRASOURCES} )
TARGET_LINK_LIBRARIES(ReadECMWF coApi coAppl coCore coUtil ${EXTRA_LIBS})
COVISE_INSTALL_TARGET(ReadECMWF)
