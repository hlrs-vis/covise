USING(NETCDF)

SET(HEADERS
  ReadWRFChem.h
)

SET(SOURCES
  ReadWRFChem.cpp
)

ADD_COVISE_MODULE(IO ReadWRFChem ${EXTRASOURCES} )
TARGET_LINK_LIBRARIES(ReadWRFChem coApi coAppl coCore coUtil ${EXTRA_LIBS})
COVISE_INSTALL_TARGET(ReadWRFChem)
