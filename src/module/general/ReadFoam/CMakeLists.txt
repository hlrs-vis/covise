USING(BOOST)
SET(HEADERS
  ReadFOAM.h
)
SET(SOURCES
  ReadFOAM.cpp
  foamtoolbox.cpp
)
ADD_COVISE_MODULE(IO ReadFoam ${EXTRASOURCES})
ADD_COVISE_COMPILE_FLAGS(ReadFoam "-pg")
ADD_COVISE_LINK_FLAGS(ReadFoam "-pg")
TARGET_LINK_LIBRARIES(ReadFoam coApi coAppl coCore ${EXTRA_LIBS})
COVISE_INSTALL_TARGET(ReadFoam)
