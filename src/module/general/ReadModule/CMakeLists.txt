USING(ASSIMP)

SET(HEADERS
  ReadModule.h
)

SET(SOURCES
  ReadModule.cpp
)

USE_VTK(OPTIONAL)
ADD_COVISE_MODULE(IO ReadModule ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(ReadModule coAlg coApi coAppl coCore ${EXTRA_LIBS})

COVISE_INSTALL_TARGET(ReadModule)
