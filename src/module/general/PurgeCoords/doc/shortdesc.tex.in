Remove unused Coordinates

PurgeCoords strips unused coordinates from geometry objects and unstructured grids.
This can be useful e.g. in the VISiT Runner modules
(where boundary condition polygon surfaces refer to a global coord list)
to generate vector fields.

