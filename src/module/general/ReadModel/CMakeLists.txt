USING(ASSIMP)

SET(HEADERS
  ReadModel.h
)

SET(SOURCES
  ReadModel.cpp
)

USE_VTK(OPTIONAL)
ADD_COVISE_MODULE(IO ReadModel ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(ReadModel coAlg coApi coAppl coCore ${EXTRA_LIBS})

COVISE_INSTALL_TARGET(ReadModel)
